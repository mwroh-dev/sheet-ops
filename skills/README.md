# Shared Skills

Top-level `skills/` is reserved for reusable shared skills.

`skills/sheet-ops/` is the single public entry surface for Codex-native
subagent orchestration.

Agent-owned single-consumer procedures belong under the owning agent subtree as
capability surfaces. Executable Go code belongs under `runtime/`.
